def main ():
	pass #нужен для того, чтобы оставить блок кода пустым

names = ('Alex', 'Rob', 'Nick') # не обязательно ставить (),
								# если использовать кортеж

#names[0] = 'sdfsdf' в кортеже нельзя присваивать элементы

print (names[0])